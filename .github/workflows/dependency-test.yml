name: Logical Dependency Demo

on: push

jobs:
  deploy-job:
    runs-on: ubuntu-latest
    needs: [build-job]
    steps:
      - name: Deploying App
        run: echo "Deploying!"

  build-job:
    runs-on: ubuntu-latest
    steps:
      - name: Building App
        run: echo "Building!"
